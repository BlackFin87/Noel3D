export default {
  plugins: {
    tailwindcss: {}, // Kích hoạt bộ công cụ giao diện Tailwind
    autoprefixer: {}, // Tự động thêm tiền tố cho code CSS để chạy tốt trên mọi trình duyệt (Chrome, Safari...)
  },
}